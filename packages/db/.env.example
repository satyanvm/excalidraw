# Database connection string for PostgreSQL (Neon)
# Add connect_timeout to handle Neon cold starts
# Format: postgresql://username:password@hostname/database?sslmode=require&connect_timeout=30
DATABASE_URL="postgresql://username:password@ep-xxx-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require&connect_timeout=30"

# JWT Secret for signing tokens
JWT_SECRET="your-secure-jwt-secret-here"

# Environment (development/production)
NODE_ENV="development"

